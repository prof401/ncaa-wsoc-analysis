# Data

Copy CSV exports from the NCAA women’s soccer scraper (`ncaa-wsoc-python`) into this directory:

- `teams.csv`
- `contests.csv`
- `scoring_summary.csv` (if you use box-score scraping)

Files matching `*.csv` are listed in the repository `.gitignore` and are not pushed to GitHub.
